const GalleryPaddingImage = ({ width, height }: { width: number; height: number }) => (
    <div className="bg-gray-900" style={{ width: width, height: height }} />
);

export default GalleryPaddingImage;
